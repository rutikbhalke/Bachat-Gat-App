name: bachat_gat
description: Bachat Gat (Self Help Group) Android Financial Management Application.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  cupertino_icons: ^1.0.4
  url_launcher: ^6.3.2
  intl: 0.20.3
  shared_preferences: ^2.0.15
  provider: ^6.1.1
  pdf: ^3.11.1
  printing: ^5.13.2
  path_provider: ^2.1.3
  share_plus: ^10.0.0
  
  # Firebase
  firebase_core: ^3.3.0
  firebase_auth: ^5.1.4
  cloud_firestore: ^5.2.0
  firebase_storage: ^12.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  generate: true
